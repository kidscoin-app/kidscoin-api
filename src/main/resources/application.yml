spring:
  application:
    name: kidscoin-api

  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/educacao_financeira}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: ${HIBERNATE_DDL_AUTO:update}
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

  sql:
    init:
      mode: always # Executa data.sql na inicialização
      continue-on-error: true # Continua mesmo se houver erro (ex: badges já existem)

  jackson:
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false

# Configurações JWT
jwt:
  secret: ${JWT_SECRET:sua-chave-secreta-minimo-256-bits-para-desenvolvimento-local-substituir-em-producao}
  access-token-expiration: 86400000 # 24 horas em milissegundos
  refresh-token-expiration: 604800000 # 7 dias em milissegundos

# Logging
logging:
  level:
    root: WARN
    com.educacaofinanceira: INFO
    org.springframework.security: WARN
    org.springframework.web: WARN
    org.hibernate: WARN
    org.hibernate.SQL: WARN
    org.hibernate.type.descriptor.sql.BasicBinder: WARN
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"

# Server
server:
  port: ${PORT:8080}
  error:
    include-message: always
    include-stacktrace: on_param
